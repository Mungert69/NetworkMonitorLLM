# Raw sources

The repository outside `LLM-Wiki/` is the default source corpus. Use this
folder only for supplementary original documents that are not part of the
repository. Once present, raw files are immutable: create a new dated file for
a revision instead of editing the prior one. This folder intentionally contains
no generated summaries.

Refer to the specific repository file and revision in a source page instead of
copying code here.
